{
  "name": "pr-checks",
  "version": "1.0.1",
  "description": "A GitHub Action that manages required checks based on GitHub repository rulesets and file changes in pull requests",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/temap/pr-checks.git"
  },
  "keywords": [
    "github",
    "actions",
    "workflow",
    "automation",
    "path-filter",
    "pr-checks"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "js-yaml": "^4.1.0",
    "minimatch": "^10.0.3"
  },
  "scripts": {
    "build": "tsc && ncc build lib/index.js -o dist --source-map --license licenses.txt",
    "watch": "tsc --watch",
    "package": "ncc build lib/index.js -o dist --source-map --license licenses.txt"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.6",
    "@typescript-eslint/eslint-plugin": "^6.19.1",
    "@typescript-eslint/parser": "^6.19.1",
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
}
